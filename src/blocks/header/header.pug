mixin header(modifier)
  .header&attributes(attributes)
    .container-fluid
      .row
        .col-12
          .header__content
            +logo(class="ml-15")
            a.btn(href="#").mr-15 О компании
